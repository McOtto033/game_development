# カード設計室 / Card Studio

新カードを定型フォームで設計し、効果・設計意図・調整履歴を開発用DBへ保存するUI。iPhone向けの公開版と、PCでファイル保存するローカル版がある。

## iPhoneで使う

[カード設計室をSafariで開く](https://mcotto033.github.io/game_development/card-studio/)。PCの起動は不要。「保存」でこの端末のブラウザ内DB（IndexedDB）へ記録する。カード案・画像・設計メモを公開サーバーへ送信しない。

- PCとは自動同期しない。「DB書き出し」でJSONをiPhoneの「ファイル」などに保存し、PC側の「取り込み」で受け渡す。
- ブラウザや端末を変えると別のDBになる。通常のSafariタブを使用し、サイトデータを削除する前にはJSONを書き出す。
- 更新前のデータは端末内にもバックアップする。「前回の保存を書き出す」から直前の版をJSONに取り出せる。履歴とバックアップは自動削除しない。
- 二つのタブで同時に編集した場合、古い版からの上書きを拒否し、入力中の内容を画面に保持する。

GitHub Pagesには画面を構成する7ファイルだけを配置する。開発用DB・バックアップ・テスト・ローカルサーバーはPagesへ配置しない。

## PCのローカル版を起動

Node.js 22以上を使い、プロジェクトのルートで実行する。追加パッケージ・外部サービスは不要。

```powershell
node development/card-studio/server.js
```

[カード設計室を開く](http://127.0.0.1:4317/)。`start.ps1` からも起動できる。終了は起動したターミナルで `Ctrl+C`。

ポートが使用中の場合は `$env:CARD_STUDIO_PORT = '4318'` を設定してから起動し、4318番を開く。同じDBを扱うサーバーは同時に1つだけ起動する。保存の競合検出は同じサーバーにつながる複数タブを対象とする。

## 最短の使い方

1. 「新しいカード」から白紙・攻撃型・支援型を選ぶ。サンプル名・数値は入力例で、採用カードではない。
2. 「基本情報」で名前、レアリティ、コスト、分類、HP/AT/AG、イラスト、得意地形、特性、初期配置制限・枚数制限を入力する。
3. 「行動・効果」で前衛・中衛・後衛・必殺技・αスキルを選び、「この枠を使う」を設定する。
4. マスまたは行動・効果から対象を指定し、効果種類・量・期間・条件を入力する。複数効果は上下の矢印で実行順を変える。
5. 「設計メモ」に意図・用途・性能評価・検証計画などを記入する。「調整履歴」に今回の変更理由や検証結果を記入する。
6. 「保存」または `Ctrl+S`。入力途中でも下書き保存でき、更新時には変更前後の記録とバックアップが残る。

既存の設計は「複製」して次のカードのたたき台にできる。複製は新しい設計IDを発行し、元の記録を保持する。ゲームカードIDと調整履歴は引き継がず、設計メモに複製元を記す。

左のライブラリでは名前・効果名/内容・設計メモ・過去の調整履歴・タグ・設計IDで検索し、下書き／検証中／採用／保留／見送りで絞り込む。五つの行動枠を区別して種類アイコンを表示する。設計上の「採用」は管理ラベルで、ゲーム本体への登録操作ではない。

## 入力様式と表示規格

本UIは開発段階の表示確認を担う。[18項目のカード情報規格](CARD_INFORMATION.md) と以下の入力規格を参照する。ローカル開発時の詳細な正本は `docs/card-presentation-standard-v1.md`、`docs/card-development-notes-v1.md`、本体UIの作業一覧は `docs/card-ui-implementation-checklist-v1.md`（この公開単位には含めない）。

| 項目 | 入力・表示 |
|---|---|
| 基本情報 | 必須18項目の枠。C/R/SR/UR、通常コストは数字、αコストは採用時の総コストとして `α3` 等 |
| イラスト | PNG/JPEG/WebP、1枚2MiBまで。開発DBに埋め込む。出典メモは開発用 |
| 分類 | 分類選択＋アイコン。クリック・タップで分類名と説明 |
| 得意地形 | 複数選択し全アイコンを表示。未選択は空欄。クリックで得意地形の説明と全地形名 |
| 特性 | 共通の一覧から選択。数値と効果量（例：`AT×1.0`）は独立して入力。クリックで共通説明と効果量。0件は「なし」 |
| 初期配置制限 | 初期配置できないマスを×で指定。衛ごとの一括指定も可。未指定は制限なし |
| 枚数制限 | 制限なし／n枚制限を選択。1〜n枚制限を合計n枚まで。各段階の上限を満たす。同名は問わず、制限なしは合算しない |
| 五つの行動枠 | 枠ごとに固有名、共通の発動時点・条件。必殺技は最大3効果、その他は最大2効果。無効は「なし」、入力済みデータは保持 |
| 効果の順序 | 上から順に実行する設計として記録。D4の同AG解決規則を変更するものではない |
| 対象 | マス指定は味方・敵の3×3で範囲内すべて／サーチ。行動・効果からの指定は攻撃してきた相手、攻撃した相手、効果の発生元、直前の効果の対象、自由入力から選択 |
| 自身の位置・相対対象 | 絶対位置にはマークを表示しない。相対位置の `●` は前衛行動なら前衛中央、中衛行動なら中衛中央、後衛行動なら後衛中央。αスキル・必殺技は配置未指定のため中衛中央で仮表示。相対対象はこの位置を基準に描画する |
| 対象図の塗り分け | 効果範囲は塗りつぶし、サーチ範囲は斜線＋破線。見出し・凡例を併記 |
| 個別の発動タイミング | 共通設定に従う／戦闘・ターン開始／ターン終了／攻撃前後／被攻撃・被ダメージ後／自由入力。対象の固定・再選択とは別 |
| 効果量 | 能力値×倍率／固定値／割合／自由記述／量なし。倍率入力は0.05刻み。自由記述は式を計算・実行しない |
| 参照元 | 自身のATは `AT`。他は `対象のAT`、`自身の最大HP` など明記 |
| 強化・弱体する項目 | AT／AG／HP（現在値）／最大HP／装甲／その他。その他は能力・効果名を自由入力。変化量の参照元とは独立し、例：`強化（AG）：AT×0.5` |
| 複数回攻撃 | 1回分の量＋1以上の整数回数。例：`攻撃：AT×0.25 / 3回` を1効果として保存。途中で倍率が変わる場合は効果を分ける |
| 状態付与 | 「付与する状態」から選択。毒／回復封じ／防御低下／接触毒／反射殻／装甲（ダメージカット）／反撃／継続回復／麻痺／バリア／その他。「その他」は独自の状態名を入力 |
| 条件付き効果量 | 一つの効果に通常量と条件成立時の量を併記。二重の実行にしない |
| 期間 | 即時は表示省略、持続は `2T` 等、常時は `常時`。必殺技の必要ターンは固有欄に `5T` 等 |

未入力項目はプレビュー下部に表示する。これは下書きの記入漏れ確認であり、ルールの整合・採点・ゲーム採用可否を自動判定するものではない。新しい地形名も追加でき、未定義の地形アイコンには仮記号を使う。地形の補正値や正式アイコンは採用時に定義する。

### 対象条件・選択・再判定の新書式

「範囲内すべて」では範囲の入力だけで対象指定を完結させる。「サーチ」を選ぶと、[効果対象の記述規格](TARGETING.md) に沿って条件、全員またはステータス順のX体を入力できる。候補区分や同値/対象不在は必要時に開く。範囲内すべてへ切り替えるとサーチ条件を適用せず隠し、サーチへ戻すと入力内容を復元する。効果の発動タイミングと対象の判定時点は両方の形式で指定できる。連続攻撃の「対象固定」と「各回の直前に再サーチ」を別の値で保存する。自然言語の条件・選び方も原文で残せる。既存カードは旧書式を保持し、「条件を分けて編集する」から移す。

### 行動・効果から対象を決める（旧書式）

「対象の指定方法」を切り替えると、マス図に代えて対象の決め方を表示する。「参照する行動・効果」で発動契機の攻撃、直前の自身の行動などを補足できる。「その他」は対象の決め方を自由入力する。対象がいない場合の代替指定も可能。マス指定へ戻すと以前の範囲・サーチ条件が復元される。

「この行動の直前の効果の対象」は、現在の表示順で一つ上の効果を参照する。先頭に移動した場合は記入漏れ欄に注意を表示する。設計用の参照指定であり、参照時点、連続攻撃の途中で対象が消えた場合の処理、3段目と同AG解決の接続などは、ゲーム本体へ採用する際に実装・検証する。

旧データの追加項目は省略でき、対象指定は従来のマス指定、攻撃回数は1回として表示する。強化・弱体する項目は勝手に補完せず、未選択として表示する。必殺技3効果の指定は2026-09-18のユーザー指示による更新で、他の枠の上限は2効果のまま。

### 付与する状態を選ぶ

効果の種類を「状態付与」にすると状態の選択欄が出る。状態名はプレビューと日本語検索に反映し、量・期間は既存の欄で設定する。例：量なしなら `状態付与（毒） / 3T`、反撃倍率を設定するなら `状態付与（反撃）：AT×0.5 / 3T`。選択肢にない状態は「その他」で記入できる。種類や状態を選び直しても入力済みの量・期間・独自名・補足は保持する。

初期候補は既存の状態定義8種に、装甲とユーザー指定の反撃を加えた設計用一覧。本体未実装の候補も含むため、本体での付与処理・値・期間の採用とは別に扱う。旧データの補足文から状態を推測して移行せず、状態が未選択なら入力待ちとして表示し、下書き保存は認める。

### 新しい効果を追加する

「効果の定義を管理」または効果内の「新しい効果種類」から、次を登録する。

- 定義ID：英小文字から始まる英数・`_`・`-`。作成後は固定。
- 効果名、種類アイコン、共通説明。
- 必要なら専用パラメーターを追加する。項目名・キー・入力型（文字／数値）の組を最大20個。例：結界名、重ね数、移動先など。

登録すると全カードから選べる。カードごとの対象・量・期間と組み合わせるため、効果のたびにコードを書き換える必要はない。定義編集は、その定義を参照する全カードの表示に影響する。IDの意味を別物に変える場合は新IDで作成する。定義変更の過去版はDBバックアップに残る。

**新効果の登録は設計データの登録。戦闘エンジンで実行する処理は別途実装する。** 任意のJavaScript実行やゲームデータへの直接書き込みは提供しない。

### 共通特性と効果量

「共通特性」から選ぶ。初期候補は「装甲」「待ち伏せ」。「共通特性を登録」で名前・共通説明を一度登録すると、別のカードでも選択できる。数値欄を維持し、別の効果量欄で `AT×1.0` などを記述する。待ち伏せなら共通説明は「常時反撃状態」、反撃倍率はカード別の効果量となる。特性を選び直しても数値と効果量を消さない。

以前の自由記入特性もそのまま読み込める。共通特性を選び直すまで元の名称・説明・数値を保持する。追加した共通定義はDB書き出しに含まれ、別端末でも取り込める。

対象図の旧データも読み込める。旧形式の相対対象は中央基準のまま保持し、描画時に行動枠へ投影する。新たに編集した相対味方範囲は `target.allyOffsets`（自身からの行・衛の差）を優先し、`ally` は旧形式互換用とする。盤外の相対指定を表示だけのために削除しない。「自身」ボタンは各行動枠の自身を選ぶ。絶対範囲の対象マスは位置補正しない。

## 保存・履歴・入出力

| 場所／操作 | 内容 |
|---|---|
| 公開版の端末内DB | カード案・メモ・履歴・追加効果定義をこのブラウザに保存。PCとは独立 |
| `data/library.json` | PCローカル版の開発用DB。カード案・メモ・履歴・追加効果定義を保存 |
| `data/backups/library-*.json` | PCローカル版では更新直前のDBを毎回複製。自動削除しない |
| 保存 | DB全体を保存。実際に変更された設計だけ履歴追加。検証結果だけの記録も可 |
| 調整履歴 | 時刻、理由、検証・学び、採否、フィールドごとの変更前後。画像の差分はデータ長を記録し、実画像の旧版はDBバックアップで保持 |
| DB書き出し | 未保存の編集・入力中の履歴メモも含むライブラリJSON。ブラウザのダウンロード先へ出力 |
| 取り込み | Card Studio v1のライブラリJSONをマージ。取り込み後に保存して確定 |
| 仕様JSON | 選択カードの表示・効果仕様と使用する効果定義。設計メモ・履歴・出典メモを除外。戦闘エンジン用の実行データではなく、DB取り込み用ファイルとも別形式 |

取り込み時、IDと内容が同一のカードは重複させない。同IDで異なるカードは**履歴を維持した取込コピー**として新しい設計IDを付ける。元カードは保持する。同じ効果IDの定義内容が異なる場合は取り込み全体を中止し、ID整理を求める。

保存中の再編集を止めて、保存成功後に画面を更新する。他タブで保存済みなら409で拒否し、編集中の値は画面に保持する。「DB書き出し」で退避→再読み込み→退避ファイルを取り込むと競合する案を別々に確認できる。

保存ボタンを押すまで編集内容はメモリ上にある。ページを離れる前に保存する。未保存時はブラウザの離脱確認を利用するが、iPhoneのアプリ終了などでは表示されないことがある。公開版の正本はIndexedDB、PCローカル版の正本はJSONファイルで、localStorageは使用しない。ライブラリ上限はカード・追加効果定義それぞれ1000件、1回の保存25MiB。画像を多く埋め込む場合は小さい画像を使用する。端末容量不足時は保存を中止し、編集中のデータを保持する。

### PCローカル版の保存ファイルが壊れた場合

1. サーバーを終了する。
2. 現在の `data/library.json` を別名で退避する。
3. `data/backups/` から復元したいJSONを確認し、そのコピーを `data/library.json` に置く。
4. サーバーを再起動する。

破損時に空DBとして上書きすることはない。復元作業前に退避し、残したい新しい編集があれば別途書き出す。`data/` はこのディレクトリーの `.gitignore` でGit対象外にしている。

## データ構造

```text
library { schemaVersion: 1, revision, cards[], definitions[], traitDefinitions?[] }
  cards[]
    id, gameCardId, artCredit, status, tags, createdAt, updatedAt
    card { name, artwork, cost, alphaCost, rarity, classification,
           terrains[], traits[], initialPlacementForbidden[], deckLimit, hp, at, ag, skills { front, middle, rear, ultimate, alpha } }
      skill { enabled, name, trigger, condition, turns, effects[] }
        effect { id, typeId, stat?, customStat?, statusId?, customStatus?, repeatCount?, target,
                 triggerTiming?, customTriggerTiming?, amount, duration, condition, alternate, details, params }
          target { mode?, source?, context?, query?, basis, ally[], enemy[], allyOffsets?[],
                   selection, count, rule, tie, fallback }
    notes { intent, strength, usage, winPlan, synergy, counter, references,
            scoring, risks, testPlan, results, learning }
    history[] { id, at, reason, result, decision, changes[] { path, before, after } }
  definitions[] { id, name, icon, description, parameters[] { key, label, type } }
  traitDefinitions[] { id, name, description }
  card.traits[] { definitionId?, name, value, effectAmount?, description }
```

カード仕様は `card`、開発情報は同階層の `notes` / `history` などに分離する。フォルダー自体もゲームの実行資産から独立する。既存カードの自動移行と戦闘エンジン用アダプターは未実装。版・記録者・実装参照は、現版では設計メモ／検証条件に記録する。

## 実装判断と次の接続点

- 2026-09-18、ユーザーの新規UI作成依頼に基づく。外部DBサービスや表計算を使う案と比較し、追加設定が不要で、可変の効果パラメーターと履歴を保ちやすいローカルJSON＋Node.jsを採用した。
- 単一設計者・ローカル利用を前提とする。複数端末で同時編集する、画像量が増える、横断集計が必要になる段階でSQLiteや共有DBへの移行を検討する。
- 表示規格の細部を将来のゲームUIへ移すときは `renderPreview`、効果入力は `effectForm`、値の検証・書式は `model.js` を参照する。既存C2の完了条件とは区別する。
- SYS/BAL/IDEAに対する位置：新カード設計を支える**開発ツールの実装**。SYS変更やBAL候補の採否には介入しない。次の接続点はカード案の作成・試用、その後の必要に応じた本体データ変換。
- 着手時は別タスクがBAL-02dを編集中だったため、本フォルダーに編集を限定した。別タスクがidleになったことを確認後、開発DB要件と引き継ぎ入口にリンクを追加した。既存ゲーム・共通検証スクリプトは変更していない。

## 検証

```powershell
node --test development/card-studio/server.test.js development/card-studio/model.test.js development/card-studio/targeting.test.js development/card-studio/restrictions.test.js
node development/card-studio/browser.test.js
node development/card-studio/static-browser.test.js
node scripts/verify.js
```

ブラウザ検証は既存のPlaywrightとEdgeを使う。`playwright` が通常のモジュール探索先にない場合は、`PLAYWRIGHT_MODULE` にインストール済みモジュールの絶対パスを渡す。テストは一時ディレクトリーに専用DBを作り、利用者の `data/library.json` を変更しない。スクリーンショットと結果は `data/verification/`。

2026-09-18の検証結果：

- サーバー6件＋モデル8件：14/14 PASS。保存・再起動・競合・バックアップ・破損データ保護・定義拡張・履歴・取り込みを確認。
- 実Edge：7系統の操作確認 PASS。作成、画像、特性/地形の説明、対象選択、順序、定義追加、設計メモ、履歴、入出力、複製、検索、競合時の保持。
- 1440／820／390／320pxで横はみ出しなし。390pxでマス・用語のタップも確認。デスクトップとモバイルの画像を目視確認。
- 標準検証：JavaScript 65ファイルの構文確認、117/117テスト PASS。本ツールは標準検証の探索対象外なので、専用テストを別途実行した。
- 開始前チェックポイント：`.autodev/checkpoints/2026-09-18T04-32-44-696Z`。

iPhone公開対応の追加検証：静的サブパス配信、端末内保存と再読込、タブ競合、バックアップ出力、別ブラウザへのJSON受け渡し、390pxでのタップ、公開先への書込通信がないことを確認。実Edgeで検証し、実機iPhone/Safariの確認は未実施。

2026-09-18改善：共通特性・独立した効果量・行動枠に合う自身位置に対応。専用単体テスト16/16、既存のローカル版／静的公開版の実Edge操作確認に加え、待ち伏せの選択、共通特性の登録、数値＋効果量の保存、前中後衛の自身位置、旧相対データを変えない投影を確認。

2026-09-18追加改善：強化・弱体する項目、行動・効果からの対象指定、攻撃回数、必殺技3効果を実装。専用21/21、標準131/131 PASS。実Edgeで入力・プレビュー・3効果の複製/上限/並べ替え・保存再読込・別DBへのJSON受け渡しを確認し、1440/820/390/320pxで横はみ出しなし。390pxの新入力欄と必殺技3効果のプレビューも目視確認。実機iPhone/Safariは未検証。

2026-09-18状態選択追加：専用22/22、標準131/131 PASS。実Edgeで毒/反撃の選択、独自状態名、効果種類の切替時の保持、日本語検索、プレビュー、保存再読込、別DBへのJSON受け渡しを確認。390/320pxで横はみ出しなし、状態入力画面の画像も確認。実機iPhone/Safariは未検証。

2026-09-18対象書式追加：専用27/27、標準131/131 PASS。実EdgeでAND/OR/除外グループ、閾値と極値、状態/特性/現在地形/得意地形/分類、全体と味方内の行動順、自然言語、全員/上位X体、固定/毎回/イベント、旧書式の変換、保存再読込とJSON受け渡しを確認。対象条件の編集でマス範囲を変えないこと、1440/820/390/320pxで横はみ出しがないこと、390pxの条件入力の画像も確認。新書式の意味・具体例・本体接続時の残件は [TARGETING.md](TARGETING.md)。実機iPhone/Safariは未検証。

2026-09-18 構築制限・対象UI簡略化：初期配置制限と枚数制限を必須18項目に追加。範囲内すべて／サーチの入力分離、絶対位置の自身マーク非表示、相対位置の表示、効果範囲とサーチ範囲の塗り分け、個別の発動時点を反映。2026-09-19の回答で、枚数制限は1〜n枚制限を合計n枚までと確定し、入力欄・プレビューの説明へ反映した。区分の入力・保存・表示に対応し、本体のデッキ判定は接続時に実装する。詳細は [カード情報規格](CARD_INFORMATION.md)。

検証：専用32/32、標準131/131 PASS。実Edgeで禁止マスの個別／衛一括／解除、枚数区分、保存再読込と別DBへの取り込み、直接範囲／サーチの切替と入力保持、個別の発動時点、絶対／相対の自身マーク、斜線／塗りつぶしを確認。390/320pxの新入力に横はみ出しなし。実機iPhone/Safariは未検証。
