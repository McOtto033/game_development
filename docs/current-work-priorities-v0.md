# 現在の残件と優先度 v0

> 2026-09-06 自動開発移行: 現在の実行順は [ROADMAP](ROADMAP.md)、成果は [試遊レビュー資料](autodev-m1-review.md) を参照する。詳細な状態と判断台帳は個人運用記録としてローカルに保持。ユーザーは未決ルールの仮決定を委任済みで、以下の「ユーザー判断が必要」「相談」は仮決定と検証へ移行する。過去の検討順を記録として残す。

## 現在地

共有資料の取り込み判断でP0だった以下は、方針採用済みかつ本体実装済み。

| 項目 | 状態 |
|---|---|
| 同AGタイブレークからコストを外す | 実装済み |
| 同速優先をマス > レアリティにする | 実装済み |
| 同AG/同マス/同レアの最大2体バッチ | 実装済み |
| 同AGバッチ内の行動保証 | 実装済み |
| D4行動優先度 | 実装済み |
| α自由指定 | 維持 |
| 6枚固定をやめ、9マス上限/30コスト内任意枚数にする | 実装済み |

9マス上限化後も、現カードプールでは標準総当たり結果は変わっていない。現在のカードは通常コスト4以上が中心で、30コスト内では5〜6枚構成が自然に残るため。

## 次にやるべきこと

| 優先 | 課題 | 今の扱い |
|---|---|---|
| P0 | 状態定義の詳細 | 次にユーザー判断が必要。毒/継続回復/回復封じ/防御低下のスタック、持続更新、解除方法を決める |
| P0 | 同名複数採用/レアリティ別枚数制限 | カードプール拡張前に判断する。現在はUI上、同じカードは1枚扱い |
| P1 | 上位/下位生成候補の原因分析 | `生成: 一点突破1`、`生成: 盤面制圧2` の上振れ、`生成: 継続ダメージ耐久1`、`生成: エース遂行2` の下振れを見る |
| P1 | `RULESET_PHASES` / `unlockPhase` | 段階解禁方針は採用済み。実装は次候補 |
| P1 | 非インタラクティブ指標 | T1/T2実質決着、敗者行動回数、初回行動前死亡率、完全ロック率を追加する |
| P2 | 行動順キュー/観戦UI | 検証UIとは分けて考える。AG数値は検証面では残す |

## 判断の進め方

- P0は、実装前にユーザーへ相談する。
- 判断に必要な検証は、先に専用シナリオまたは総当たりを走らせて結果を提示する。
- 状態定義はD4同時解決の順序とは分離する。毒や継続回復のスタック可否は、状態そのものの仕様として扱う。

## 直近の推奨順

1. 状態定義セッションに入る。
2. 毒、継続回復、回復封じ、防御低下について、スタック/上書き/強い方優先/持続更新の候補を比較する。
3. 必要なら専用シナリオを作り、判断材料を出す。
4. ユーザー判断後、本体実装へ反映する。
